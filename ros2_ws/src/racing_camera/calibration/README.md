# Camera calibration

Store the selected competition camera's reviewed intrinsic and extrinsic calibration here. Do not commit a calibration copied from another physical camera.

The hardware adapter must publish the topic and frame contract in `config/topic_contract.yaml`. The simulator must expose the same names and compatible image encodings.
